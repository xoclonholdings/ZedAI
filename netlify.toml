[build]
  base = "client"
  command = "npm install --prefix ../shared && npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  VITE_API_BASE_URL = "https://api.zar-ai.online"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
