{
  "scripts": {
    "build:frontend": "cd ../zed-web && npm ci && npm run build",
    "build:backend": "npm ci",
    "build": "npm run build:frontend && npm run build:backend",
    "start:backend": "node server.js",
    "start:all": "npm run build && concurrently \"npm run start:backend\""
  },
  "engines": {
    "node": ">=22.12.0",
    "npm": ">=10.8.0"
  }
}